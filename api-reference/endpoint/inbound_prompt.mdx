api: "POST https://your-api-endpoint/inbound/update"
description: "Update the prompt for a given inbound phone number."
---

### Headers

<ParamField header="x-api-key" type="string">
  Your API key for authentication.
</ParamField>

### Body

<ParamField body="phone_number" type="string">
  The phone number associated with the prompt.
  - Format: "XXXXXXXXXX" or "XXX-XXX-XXXX"
</ParamField>

<ParamField body="prompt" type="string">
  The new prompt for the given phone number. The prompt shouldn't exceed 5000 characters.
  Note: Ensure the prompt is clear and relevant to your use case.
</ParamField>

### Response

<ResponseField name="id" type="integer">
  The unique ID associated with the inbound record.
</ResponseField>

<ResponseField name="created_at" type="string">
  The creation timestamp of the inbound record.
  - Example: "2023-10-10T12:00:00Z"
</ResponseField>

<ResponseField name="phone_number" type="string">
  The updated phone number.
  - Example: "1234567890"
</ResponseField>

<ResponseField name="prompt" type="string">
  The updated prompt text.
</ResponseField>

<ResponseField name="status" type="string">
  The status of the inbound record. Typically "active".
</ResponseField>

<RequestExample>

```javascript UpdatePrompt.js
// Headers
const headers = {
  'x-api-key': "YOUR-API-KEY-HERE",
};

// Data
const data = {
    "phone_number": "1234567890",
    "prompt": "Your new prompt here...",
}
;

// API request
await axios.post("https://your-api-endpoint/inbound/update", data, { headers });
```

```python UpdatePrompt.py
# Headers
headers = {
    'x-api-key': 'YOUR-API-KEY-HERE'
}

# Data
data = {
    'phone_number': '1234567890',
    'prompt': 'Your new prompt here...',
}

# API request
response = requests.post('https://your-api-endpoint/inbound/update', json=data, headers=headers)
```
</RequestExample>

<ResponseExample>

```json Response
{
  "id": 1,
  "created_at": "2023-10-10T12:00:00Z",
  "phone_number": "1234567890",
  "prompt": "New Prompt Text",
  "status": "active"
}
```

</ResponseExample>

Make sure to replace "https://your-api-endpoint" with the actual base URL of your API. This MDX format should be compatible with your Mintlify documentation generator.
