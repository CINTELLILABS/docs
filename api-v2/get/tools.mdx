---
title: "List Tools"
api: "GET https://api.bland.ai/v2/tools"
description: "Retrieve a paginated list of your tools. Returns tools that are not tied to a specific resource connection."
---

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Query Parameters

<ParamField query="page" type="integer">
  Page number for pagination. Minimum `1`. Defaults to `1`.
</ParamField>

<ParamField query="limit" type="integer">
  Number of results per page. Minimum `1`, maximum `1000`. Defaults to `50`.
</ParamField>

<ParamField query="search" type="string">
  Filter results by searching the tool's `name` and `description` fields. The match is case-sensitive.
</ParamField>

<ParamField query="agent_id" type="string">
  Return only tools created with this agent's UUID. Omit to list every tool in your account. Returns `400 INVALID_PARAMETER` if the value is not a UUID.
</ParamField>

<ParamField query="orderBy" type="string">
  Field to sort results by. Currently only `"created_at"` is supported.
</ParamField>

<ParamField query="orderDirection" type="string">
  Sort direction. Allowed values: `"asc"`, `"desc"`. Defaults to `"desc"`.
</ParamField>

<Info>
  This endpoint only returns tools that are **not** tied to a specific resource connection (`resource_id: null`). Tools created as part of a connected integration resource are excluded.
</Info>

<Note>
  Query parameters are validated strictly. Any parameter not listed above returns `400 INVALID_PARAMETER`.
</Note>

### Response

<ResponseField name="data.tools" type="array">
  Array of tool rows. Each row is the stored record: the tool definition you sent at creation is nested under `tool`, and the row's own columns sit beside it.

  <Expandable title="tool row">
    <ResponseField name="id" type="string">
      The tool's UUID. Not prefixed with `TL-`. Pass it as-is to [Update Tool](/api-v2/post/tools-tool-id) and [Delete Tool](/api-v2/delete/tools-tool-id).
    </ResponseField>
    <ResponseField name="label" type="string | null">
      Human-readable label, if set.
    </ResponseField>
    <ResponseField name="tool" type="object">
      The tool definition: `name`, `description`, `integration`, `action`, and any optional fields you set (`input_schema`, `body`, `speech`, `timeout`, `cache`, `response_data`, `max_retries`, `cooldown`, `extractVarSettings`, `resource_id`).
    </ResponseField>
    <ResponseField name="staging_tool" type="object | null">
      The draft definition, if one was saved.
    </ResponseField>
    <ResponseField name="user_id" type="string">
      UUID of the owning organization.
    </ResponseField>
    <ResponseField name="resource_id" type="null">
      Always `null` on this endpoint (see the note above).
    </ResponseField>
    <ResponseField name="agent_id" type="string | null">
      UUID of the agent the tool was created for, or `null` for org-wide tools.
    </ResponseField>
    <ResponseField name="public" type="boolean">
      Whether the tool is publicly visible.
    </ResponseField>
    <ResponseField name="is_active" type="boolean">
      Whether the tool is active and available for use.
    </ResponseField>
    <ResponseField name="is_draft" type="boolean">
      Whether the tool is in draft state.
    </ResponseField>
    <ResponseField name="created_at" type="string">
      ISO 8601 timestamp of when the tool was created.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="data.pagination" type="object">
  Pagination metadata.

  <Expandable title="pagination object">
    <ResponseField name="page" type="number">
      The current page number.
    </ResponseField>
    <ResponseField name="limit" type="number">
      Number of results per page.
    </ResponseField>
    <ResponseField name="hasMore" type="boolean">
      Whether there are additional pages of results.
    </ResponseField>
    <ResponseField name="nextPage" type="number | null">
      The next page number, or `null` if there are no more pages.
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="errors" type="null|array">
  `null` on success, or a list of error objects if the request failed.
</ResponseField>

<ResponseExample>
```json Response (Success)
{
  "data": {
    "tools": [
      {
        "id": "8f4c1d2e-6b7a-4c3d-9e0f-1a2b3c4d5e6f",
        "label": null,
        "tool": {
          "name": "SendSlackMessage",
          "description": "Sends a message to a Slack channel",
          "integration": "slack",
          "action": "send_message",
          "input_schema": {
            "type": "object",
            "properties": {
              "channel": { "type": "string" },
              "message": { "type": "string" }
            },
            "required": ["channel", "message"]
          },
          "speech": "Sending that message to Slack now.",
          "timeout": 10000
        },
        "staging_tool": null,
        "user_id": "0c1d2e3f-4a5b-4c6d-8e7f-9a0b1c2d3e4f",
        "resource_id": null,
        "agent_id": null,
        "public": false,
        "is_active": true,
        "is_draft": false,
        "created_at": "2025-09-23T15:13:36.348Z"
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 50,
      "hasMore": false,
      "nextPage": null
    }
  },
  "errors": null
}
```

```json Error (INVALID_PARAMETER)
{
  "data": null,
  "errors": [
    {
      "error": "INVALID_PARAMETER",
      "message": "/limit: Expected integer to be less or equal to 1000"
    }
  ]
}
```
</ResponseExample>

---

Docs for agents: [llms.txt](/llms.txt)
