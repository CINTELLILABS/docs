---
title: "Get Batch"
api: "GET https://api.bland.ai/v2/batches/{batch_id}"
description: "Retrieve metadata for a specific batch. This does not include logs, use the logs endpoint for that."
---

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Path Parameters

<ParamField path="batch_id" type="string" required>
  The unique identifier of the batch you want to fetch.
</ParamField>

### Response

The stored batch row. If the batch does not exist or belongs to another organization, the request still returns `200` with `"data": null` rather than a 404.

<ResponseField name="data" type="object | null">
  Metadata and configuration details of the batch, or `null` if not found.
</ResponseField>

<ResponseField name="data.id" type="string">
  The unique identifier of the batch.
</ResponseField>

<ResponseField name="data.user_id" type="string">
  The ID of the org that created the batch.
</ResponseField>

<ResponseField name="data.base_prompt" type="string">
  A JSON string of the `global` object the batch was created with.
</ResponseField>

<ResponseField name="data.created_at" type="string (ISO 8601)">
  The timestamp of when the batch was created.
</ResponseField>

<ResponseField name="data.call_objects" type="array | null">
  The full `call_objects` array the batch was created with. `null` for batches created from a `file_id`.
</ResponseField>

<ResponseField name="data.description" type="string">
  A human-readable label or description for the batch.
</ResponseField>

<ResponseField name="data.batch_status" type="string | object">
  `"initializing"` until the batch starts dispatching, then an object with `calls_requested`, `calls_in_progress`, `calls_completed`, `calls_failed`, and `calls_failed_errors`.
</ResponseField>

<ResponseField name="data.calls_in_retry" type="array">
  Call IDs currently being retried.
</ResponseField>

<ResponseField name="data.label" type="string | null">
  A JSON string of the `column_mapping` used for file batches, otherwise `null`.
</ResponseField>

<ResponseField name="data.agent_id" type="string | null">
  The `global.agent_id` the batch was created with, if any.
</ResponseField>

<ResponseField name="data.campaign_id" type="string | null">
  Always `null` for batches created through this API.
</ResponseField>

<ResponseField name="data.call_params" type="string | null">
  Unused by v2 batches. Always `null`.
</ResponseField>

<ResponseField name="data.call_data" type="string | null">
  Unused by v2 batches. Always `null`.
</ResponseField>

<ResponseField name="data.endpoint_code" type="string">
  Always `"api"`.
</ResponseField>

<ResponseField name="errors" type="null">
  Always `null` on success.
</ResponseField>

### Errors

| Status | `error` | When |
| --- | --- | --- |
| 500 | `BATCH_V2_FETCH_ERROR` | Unexpected failure |

<RequestExample>

```bash cURL
curl -X GET https://api.bland.ai/v2/batches/a9a033b7-44d8-40c6-aaec-42cd43106273 \
  -H "authorization: YOUR_API_KEY"
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": {
    "id": "a9a033b7-44d8-40c6-aaec-42cd43106273",
    "user_id": "657ad643-92ca-477c-97cc-9d8e6e74d7af",
    "campaign_id": null,
    "call_params": null,
    "base_prompt": "{\"record\":true,\"task\":\"Say hello to the user\"}",
    "endpoint_code": "api",
    "created_at": "2025-05-08T19:28:12.627Z",
    "label": null,
    "call_data": null,
    "batch_status": {
      "calls_requested": 1,
      "calls_in_progress": [],
      "calls_completed": ["a24616b4-be8f-42e4-be29-4c5ec005756c"],
      "calls_failed": 0,
      "calls_failed_errors": []
    },
    "calls_in_retry": [],
    "call_objects": [
      {
        "task": "Say hello to the user",
        "phone_number": "+1234567890"
      }
    ],
    "description": "Untitled Batch",
    "agent_id": null
  },
  "errors": null
}
```

```json Not Found
{
  "data": null,
  "errors": null
}
```
</ResponseExample>

---

Docs for agents: [llms.txt](/llms.txt)
