---
title: "Rate Voice"
api: "POST https://api.bland.ai/v1/voices/{id}/rate"
description: "Give a public library voice a 1-5 star rating."
---

## Overview

Records your rating (1 to 5) for a voice in the Bland library. Ratings are aggregated and surfaced as `average_rating` and `total_ratings` on each voice. Each user can have at most one active rating per voice. Sending a new rating overwrites the previous one. Use [Delete Voice Rating](/api-v1/delete/voices-id-rate) to clear it.

This call is throttled to 20 ratings per minute per user, falling back to the org ID when no user ID is available.

---

## Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

---

## Path Parameters

<ParamField path="id" type="string" required>
  UUID of the voice to rate.
</ParamField>

---

## Body Parameters

<ParamField body="rating" type="integer" required>
  Rating value, 1 to 5.
</ParamField>

---

## Response

<ResponseField name="status" type="string">
  `success` on success.
</ResponseField>

<ResponseField name="total_ratings" type="number">
  The voice's new total rating count.
</ResponseField>

<ResponseField name="average_rating" type="number">
  The voice's new average rating.
</ResponseField>

### Errors

| Code | HTTP | Meaning |
|---|---|---|
| `ERROR_RATING_VOICE` | 400 | `rating` is not an integer from 1 to 5, or the update failed validation. |
| `CANNOT_RATE_OWN_VOICE` | 403 | You own or created the voice. |
| `VOICE_NOT_FOUND` | 404 | The ID is invalid, or the voice is private, deleted, or unknown. |
| `RATE_LIMITED` | 429 | The rating limit was exceeded. `Retry-After` is set. |
| `UNEXPECTED_ERROR` | 500 | The rating could not be saved. |

<ResponseExample>
```json Response
{
  "status": "success",
  "total_ratings": 8,
  "average_rating": 4.875
}
```
</ResponseExample>

---

Docs for agents: [llms.txt](/llms.txt)
