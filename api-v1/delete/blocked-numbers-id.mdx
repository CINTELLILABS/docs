---
title: "Delete Block Rule"
api: "DELETE https://api.bland.ai/v1/blocked_numbers/{block_id}"
description: "Permanently delete a block rule."
---

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Path Parameters

<ParamField path="block_id" type="string" required>
  The ID of the block rule to delete, as returned by [Create Block Rules](/api-v1/post/blocked-numbers).
</ParamField>

### Response

<ResponseField name="data" type="object">
  Confirmation message and ID of the deleted rule.
</ResponseField>

<ResponseField name="data.message" type="string">
  Success message indicating the block rule was deleted.
</ResponseField>

<ResponseField name="data.deleted_block_id" type="string">
  The ID of the block rule that was removed.
</ResponseField>

<ResponseField name="errors" type="null|array">
  `null` on success, or a list of error objects (`404 BLOCK_NOT_FOUND`, `500 SERVICE_ERROR` if deletion failed).
</ResponseField>

<ResponseExample>
```json Response
{
  "data": {
    "message": "Block rule deleted successfully",
    "deleted_block_id": "c1f0a5e2-7d5b-4a3c-9f61-2b8e4d7a9c10"
  },
  "errors": null
}
```
</ResponseExample>
---

Docs for agents: [llms.txt](/llms.txt)
