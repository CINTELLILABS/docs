---
title: "List Conversations"
api: "GET https://api.bland.ai/v1/sms/conversations"
description: "Retrieve a paginated list of SMS conversations for your organization, with filtering, sorting, and metadata."
---

<Note>
  Messaging is an Enterprise feature. Configuring a number for messaging (`POST /v1/sms/number/update`) and sending (`/send`, `/batch`) require an Enterprise plan. This endpoint itself is not plan-gated.
</Note>

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Query Parameters

<ParamField query="limit" type="number" required={false}>
  Maximum number of conversations to return, from `0` to `1000`. Defaults to `1000`.
</ParamField>

<ParamField query="from" type="number" required={false}>
  Zero-based offset of the first conversation to return. Defaults to `0`. Combine with `limit` to page through results.
</ParamField>

<ParamField query="sort_by" type="string" required={false}>
  Field to sort by. One of `created_at`, `updated_at`, `last_message_at`, or `message_count`. Defaults to `created_at`.
</ParamField>

<ParamField query="ascending" type="boolean" required={false}>
  Sort direction. `true` for ascending, `false` for descending. Defaults to `false`.
</ParamField>

<ParamField query="filters" type="object[]" required={false}>
  One or more filter objects, each passed as a JSON-encoded string (`filters[]={"field":"is_active","operator":"equals","value":true}`), or a single `filters` parameter containing a JSON array. Each object has `field`, `operator`, and `value`.

  Supported `field` values and the operators each accepts:
  - `conversation_id`: `equals`
  - `user_number`, `agent_number`: `equals`, `contains`
  - `is_active`: `equals` (boolean)
  - `status`: `equals`, `not`. Value is one of `active`, `completed`, `timed_out`, or `inactive`
  - `channel`: `equals`. Value is one of `SMS`, `WHATSAPP`, or `IMESSAGE`
  - `curr_pathway_id`: `equals`, `isEmpty`, `isNotEmpty`
  - `created_at`, `updated_at`, `last_message_at`: `equals` (matches the whole calendar day), `gt`, `gte`, `lt`, `lte`
  - `message_count`: `equals`, `gt`, `gte`, `lt`, `lte`
  - `pathway_tags`: `hasSome`, `none`. Value is a JSON-encoded tag object or array of tag objects
  - `variables`, `citation_variables`: `hasSome`, `none` with a JSON-encoded `{"key": value}` object; `citation_variables` also accepts `isEmpty` and `isNotEmpty`
  - `disposition_logs`: `hasSome`, `none` with a JSON-encoded `{"key": value}` object matched against outcome results, or `isEmpty` / `isNotEmpty`
  - Other scalar columns of the conversation (for example `current_node_id`, `curr_pathway_version`, `timed_out_at`): `equals`, `not`, `in`, `notIn`, `contains`, `startsWith`, `endsWith`, `isEmpty`, `isNotEmpty`, `gt`, `gte`, `lt`, `lte`

  The full operator set is `equals`, `not`, `in`, `notIn`, `lt`, `lte`, `gt`, `gte`, `contains`, `startsWith`, `endsWith`, `isEmpty`, `isNotEmpty`, `hasSome`, `hasEvery`, `none`. A filter with an unknown field or an operator the field does not support is ignored rather than rejected.
</ParamField>

<ParamField query="mode" type="string" required={false}>
  How multiple filters combine. `AND` or `OR`. Defaults to `AND`.
</ParamField>

### Response

<ResponseField name="data" type="array">
  A list of conversations matching the query.
</ResponseField>

<ResponseField name="data[].id" type="string">
  Unique ID of the conversation.
</ResponseField>

<ResponseField name="data[].created_at" type="string">
  ISO timestamp when the conversation was created.
</ResponseField>

<ResponseField name="data[].updated_at" type="string">
  ISO timestamp when the conversation was last updated.
</ResponseField>

<ResponseField name="data[].user_number" type="string">
  The user-facing phone number in the conversation.
</ResponseField>

<ResponseField name="data[].agent_number" type="string">
  The assistant's phone number in the conversation.
</ResponseField>

<ResponseField name="data[].variables" type="object">
  Arbitrary variables associated with the conversation.
</ResponseField>

<ResponseField name="data[].current_node_id" type="string">
  ID of the current node in the conversational pathway.
</ResponseField>

<ResponseField name="data[].curr_pathway_id" type="string|null">
  The UUID of the current pathway being used in this conversation.
</ResponseField>

<ResponseField name="data[].is_active" type="boolean">
  Indicates whether the conversation is still active.
</ResponseField>

<ResponseField name="data[].timed_out_at" type="string|null">
  ISO timestamp when the conversation timed out due to inactivity, or `null`.
</ResponseField>

<ResponseField name="data[].ended_by_end_call_node" type="boolean">
  `true` when the conversation was ended by an End Call node in the pathway.
</ResponseField>

<ResponseField name="data[].message_count" type="number">
  Number of messages in the conversation.
</ResponseField>

<ResponseField name="data[].last_message" type="string|null">
  The content of the most recent message, if available.
</ResponseField>

<ResponseField name="data[].last_message_at" type="string|null">
  The timestamp of the most recent message.
</ResponseField>

<ResponseField name="data[].pathway_tags" type="array|null">
  Tags from pathway nodes visited during the conversation.
</ResponseField>

<ResponseField name="data[].effective_channel" type="string|null">
  The resolved delivery channel: `"sms"`, `"rcs"`, `"whatsapp"`, or `"imessage"`.
</ResponseField>

<ResponseField name="data[].summary" type="string|null">
  AI-generated conversation summary, if available.
</ResponseField>

<ResponseField name="data[].citation_variables" type="object|null">
  Extracted citation variable values, if citation schemas were configured.
</ResponseField>

<ResponseField name="data[].disposition_tag" type="string|null">
  The first successful outcome result tag, if outcomes were configured.
</ResponseField>

<ResponseField name="errors" type="null|array">
  `null` on success, or a list of errors on failure.
</ResponseField>

<ResponseField name="pagination.totalItems" type="number">
  Total number of conversations matching the query.
</ResponseField>

<ResponseField name="pagination.totalPages" type="number">
  Total number of pages available at the current `limit`.
</ResponseField>

<ResponseField name="pagination.currentPage" type="number">
  The current page, derived from `from` and `limit`.
</ResponseField>

<ResponseField name="pagination.pageSize" type="number">
  The `limit` applied to this response.
</ResponseField>

### Errors

- `400` `INVALID_PARAMETER`: a query parameter has the wrong type or is out of range (for example `limit` above `1000`).
- `500` `ERROR_GETTING_CONVERSATIONS`: unexpected server error.

<ResponseExample>
```json Response
{
  "data": [
    {
      "id": "convo_abc123",
      "created_at": "2024-11-20T14:01:00.000Z",
      "updated_at": "2024-11-21T09:12:00.000Z",
      "user_number": "+15550001111",
      "agent_number": "+15552223333",
      "variables": { "campaign_id": "xyz789" },
      "current_node_id": "node_intro",
      "is_active": true,
      "timed_out_at": null,
      "ended_by_end_call_node": false,
      "message_count": 5,
      "last_message": "Hi there, how can I help you?",
      "last_message_at": "2024-11-21T09:12:00.000Z",
      "curr_pathway_id": "pathway_abc123",
      "pathway_tags": [{"name": "Greeting", "color": "#4287f5"}],
      "summary": null,
      "citation_variables": null,
      "disposition_tag": null,
      "effective_channel": "sms"
    }
  ],
  "errors": null,
  "pagination": {
    "totalItems": 42,
    "totalPages": 5,
    "currentPage": 1,
    "pageSize": 10
  }
}
```

</ResponseExample>


---

Docs for agents: [llms.txt](/llms.txt)
