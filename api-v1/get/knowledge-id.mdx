---
title: "Get Knowledge Base"
api: "GET https://api.bland.ai/v1/knowledge/{knowledge_base_id}"
description: "Retrieves a specific knowledge base by ID."
---

Returns detailed information about a specific knowledge base, including its current status, metadata, and file information (if applicable).

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Path Parameters

<ParamField path="knowledge_base_id" type="string" required>
  The unique identifier of the knowledge base to retrieve.
</ParamField>

### Response

<ResponseField name="data" type="object">
  The knowledge base object.

  <ResponseField name="data.id" type="string">
    Unique identifier for the knowledge base.
  </ResponseField>

  <ResponseField name="data.name" type="string">
    Name of the knowledge base.
  </ResponseField>

  <ResponseField name="data.description" type="string">
    Description of the knowledge base (if provided).
  </ResponseField>

  <ResponseField name="data.status" type="string">
    Current status: `"PROCESSING"`, `"COMPLETED"`, `"FAILED"`, or `"DELETED"`.
  </ResponseField>

  <ResponseField name="data.type" type="string">
    Type of knowledge base: `"FILE"`, `"WEB_SCRAPE"`, `"TEXT"`, or `"INTEGRATION"`.
  </ResponseField>

  <ResponseField name="data.source_urls" type="string">
    For web scrape type: the scraped URLs as a JSON-encoded array string.
  </ResponseField>

  <ResponseField name="data.base_url" type="string">
    Base URL for web scrape type.
  </ResponseField>

  <ResponseField name="data.metadata" type="object">
    Source metadata recorded during ingestion. May be `null`.
  </ResponseField>

  <ResponseField name="data.current_version_id" type="string">
    ID of the content version currently served.
  </ResponseField>

  <ResponseField name="data.chunk_count" type="number">
    Number of indexed chunks. `null` until processing completes.
  </ResponseField>

  <ResponseField name="data.sync_enabled" type="boolean">
    Whether scheduled re-sync is enabled (web scrape and integration sources).
  </ResponseField>

  <ResponseField name="data.last_sync_at" type="string">
    ISO timestamp of the last sync, or `null`.
  </ResponseField>

  <ResponseField name="data.persona_id" type="string">
    Set on persona-owned knowledge bases. Always `null` for knowledge bases you created.
  </ResponseField>

  <ResponseField name="data.ranking_config" type="object">
    Per-source retrieval configuration, or `null` for default hybrid retrieval.
  </ResponseField>

  <ResponseField name="data.created_at" type="string">
    ISO timestamp of creation.
  </ResponseField>

  <ResponseField name="data.updated_at" type="string">
    ISO timestamp of last update.
  </ResponseField>

  <ResponseField name="data.error_message" type="string">
    Error message if status is `"FAILED"`.
  </ResponseField>

  <ResponseField name="data.current_version" type="object">
    Summary of the current version: `id`, `source_file_id`, `markdown_file_id`, `source_urls`, `created_at`.
  </ResponseField>

  <ResponseField name="data.processing" type="object">
    Live conversion progress, present only while `status` is `"PROCESSING"`: `state`, `attempts`, `max_attempts`, `pages_done`, `total_pages`, `percent`, `eta_seconds`, `last_error`.
  </ResponseField>

  <ResponseField name="data.query_count_30d" type="number">
    Queries against this knowledge base in the last 30 days. Companion counters: `gap_count_30d`, `covered_count_30d`, `coverage_pct_30d`, `updated_coverage_pct_30d`, `seen_in_playground`, `seen_in_api`.
  </ResponseField>

  <ResponseField name="data.kb_item_scopes" type="object">
    Personas and pathway nodes that reference this knowledge base. Omitted when unavailable.
  </ResponseField>

  <ResponseField name="data.file" type="object">
    File information for file-type knowledge bases.

    <ResponseField name="data.file.file_name" type="string">
      Original filename.
    </ResponseField>

    <ResponseField name="data.file.file_size" type="number">
      File size in bytes.
    </ResponseField>

    <ResponseField name="data.file.file_type" type="string">
      MIME type of the file.
    </ResponseField>
  </ResponseField>
</ResponseField>

<ResponseField name="errors" type="null">
  Will be `null` on successful request.
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET https://api.bland.ai/v1/knowledge/3f1c2a9e-7b4d-4c1e-9a2b-1d5e6f7a8b9c \
  -H "authorization: YOUR_API_KEY"
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "data": {
    "id": "3f1c2a9e-7b4d-4c1e-9a2b-1d5e6f7a8b9c",
    "name": "Company FAQs",
    "description": "Frequently asked questions and policies",
    "status": "COMPLETED",
    "type": "FILE",
    "source_urls": null,
    "base_url": null,
    "metadata": null,
    "current_version_id": "8c7d6e5f-4a3b-4c2d-9e1f-0a1b2c3d4e5f",
    "created_at": "2025-01-15T10:30:00Z",
    "updated_at": "2025-01-15T10:32:00Z",
    "error_message": null,
    "chunk_count": 42,
    "sync_enabled": false,
    "last_sync_at": null,
    "persona_id": null,
    "ranking_config": null,
    "file": {
      "file_name": "company_faqs.pdf",
      "file_size": 2048576,
      "file_type": "application/pdf"
    },
    "current_version": {
      "id": "8c7d6e5f-4a3b-4c2d-9e1f-0a1b2c3d4e5f",
      "source_file_id": "0d1e2f3a-4b5c-4d6e-8f9a-0b1c2d3e4f5a",
      "markdown_file_id": "1e2f3a4b-5c6d-4e7f-9a0b-1c2d3e4f5a6b",
      "source_urls": null,
      "created_at": "2025-01-15T10:30:00Z"
    },
    "kb_item_scopes": { "personas": [], "pathway_nodes": [] },
    "query_count_30d": 128,
    "gap_count_30d": 3,
    "covered_count_30d": 125,
    "coverage_pct_30d": 0.9765625,
    "updated_coverage_pct_30d": 0.984375,
    "seen_in_playground": true,
    "seen_in_api": true,
    "kb_service_capabilities": { "anchor_retrieval": true }
  },
  "errors": null
}
```

```json Not Found Response
{
  "data": null,
  "errors": [
    {
      "error": "KB_ERROR",
      "message": "KB not found"
    }
  ]
}
```

</ResponseExample>

---

Docs for agents: [llms.txt](/llms.txt)
