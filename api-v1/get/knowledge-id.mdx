---
title: "Get Knowledge Base"
api: "GET https://api.bland.ai/v1/knowledge/{knowledge_base_id}"
description: "Retrieves a specific knowledge base by ID."
---

Returns detailed information about a specific knowledge base, including its current status, metadata, and file information (if applicable).

### Headers

<ParamField header="authorization" type="string" required>
  Your API key for authentication.
</ParamField>

### Path Parameters

<ParamField path="knowledge_base_id" type="string" required>
  The unique identifier of the knowledge base to retrieve.
</ParamField>

### Response

<ResponseField name="data" type="object">
  The knowledge base object.

  <ResponseField name="data.id" type="string">
    Unique identifier for the knowledge base.
  </ResponseField>

  <ResponseField name="data.name" type="string">
    Name of the knowledge base.
  </ResponseField>

  <ResponseField name="data.description" type="string">
    Description of the knowledge base (if provided).
  </ResponseField>

  <ResponseField name="data.status" type="string">
    Current status: `"PROCESSING"`, `"COMPLETED"`, `"FAILED"`, or `"DELETED"`.
  </ResponseField>

  <ResponseField name="data.type" type="string">
    Type of knowledge base: `"FILE"`, `"WEB_SCRAPE"`, or `"TEXT"`.
  </ResponseField>

  <ResponseField name="data.source_urls" type="string">
    Source URLs for web scrape type (comma-separated).
  </ResponseField>

  <ResponseField name="data.base_url" type="string">
    Base URL for web scrape type.
  </ResponseField>

  <ResponseField name="data.created_at" type="string">
    ISO timestamp of creation.
  </ResponseField>

  <ResponseField name="data.updated_at" type="string">
    ISO timestamp of last update.
  </ResponseField>

  <ResponseField name="data.error_message" type="string">
    Error message if status is `"FAILED"`. Knowledge bases with status `"FAILED"` are excluded from agent queries.
  </ResponseField>

  <ResponseField name="data.processing" type="object">
    Conversion progress for a PDF that is still being processed. Only present while status is `"PROCESSING"` and a conversion is underway. Poll this endpoint to track progress.

    <ResponseField name="data.processing.state" type="string">
      `"running"` while a worker is converting the document, `"pending"` while the job waits to be picked up.
    </ResponseField>

    <ResponseField name="data.processing.pages_done" type="number">
      Number of pages converted so far.
    </ResponseField>

    <ResponseField name="data.processing.total_pages" type="number">
      Total pages in the document. `null` until the page count has been read.
    </ResponseField>

    <ResponseField name="data.processing.percent" type="number">
      Progress from 0 to 100. `null` until the page count is known.
    </ResponseField>

    <ResponseField name="data.processing.eta_seconds" type="number">
      Estimated seconds until conversion finishes. `null` until enough pages have been converted to estimate a rate.
    </ResponseField>

    <ResponseField name="data.processing.attempts" type="number">
      Number of conversion attempts started, including the current one.
    </ResponseField>

    <ResponseField name="data.processing.max_attempts" type="number">
      Maximum conversion attempts before the knowledge base is marked `"FAILED"`.
    </ResponseField>

    <ResponseField name="data.processing.last_error" type="string">
      Error from the most recent failed attempt, if any. `null` when no attempt has failed.
    </ResponseField>
  </ResponseField>

  <ResponseField name="data.file" type="object">
    File information for file-type knowledge bases.

    <ResponseField name="data.file.file_name" type="string">
      Original filename.
    </ResponseField>

    <ResponseField name="data.file.file_size" type="number">
      File size in bytes.
    </ResponseField>

    <ResponseField name="data.file.file_type" type="string">
      MIME type of the file.
    </ResponseField>
  </ResponseField>
</ResponseField>

<ResponseField name="errors" type="null">
  Will be `null` on successful request.
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET https://api.bland.ai/v1/knowledge/kb_01H8X9QK5R2N7P3M6Z8W4Y1V5T \
  -H "authorization: YOUR_API_KEY"
```

</RequestExample>

<ResponseExample>

```json Success Response
{
  "data": {
    "id": "kb_01H8X9QK5R2N7P3M6Z8W4Y1V5T",
    "name": "Company FAQs",
    "description": "Frequently asked questions and policies",
    "status": "COMPLETED",
    "type": "FILE",
    "created_at": "2025-01-15T10:30:00Z",
    "updated_at": "2025-01-15T10:32:00Z",
    "file": {
      "file_name": "company_faqs.pdf",
      "file_size": 2048576,
      "file_type": "application/pdf"
    }
  },
  "errors": null
}
```

```json Processing Response
{
  "data": {
    "id": "kb_01H8X9QK5R2N7P3M6Z8W4Y1V5T",
    "name": "Evidence of Coverage",
    "description": "Plan coverage details",
    "status": "PROCESSING",
    "type": "FILE",
    "created_at": "2025-01-15T10:30:00Z",
    "updated_at": "2025-01-15T10:31:00Z",
    "processing": {
      "state": "running",
      "pages_done": 45,
      "total_pages": 208,
      "percent": 22,
      "eta_seconds": 620,
      "attempts": 1,
      "max_attempts": 3,
      "last_error": null
    },
    "file": {
      "file_name": "evidence_of_coverage.pdf",
      "file_size": 8388608,
      "file_type": "application/pdf"
    }
  },
  "errors": null
}
```

```json Not Found Response
{
  "data": null,
  "errors": [
    {
      "error": "KB_ERROR",
      "message": "KB not found or access denied"
    }
  ]
}
```

</ResponseExample>

---

Docs for agents: [llms.txt](/llms.txt)
