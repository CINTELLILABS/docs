---
title: "Instinct"
description: "Integrate Instinct with Bland: give your Instinct agent its own phone number and let it call and text on your behalf"
hidden: true
---

Instinct is a personal AI agent you text (iMessage or WhatsApp) or call. It runs on its own persistent cloud computer with a browser and stored credentials, and it acts on your behalf across the services you connect. Instinct does not document MCP or custom API integrations, so it works with Bland the way it works with any web service: you give it the [REST API](/platform/agent-quickstart) and a credential, or you let it sign up for one itself.

There are two ways to connect, depending on whether you already have a Bland account. Setting up a different assistant? See [Personal AI Agents](/integrations/mcp/personal-agents).

## Prerequisites

- Access to Instinct. It is invite-only at the time of writing.
- A payment card for the [Agent Phone Plan](/platform/agent-phone-plan) (\$29.99/month, includes a US phone number)

## Option 1: You have a Bland API key

Text Instinct, and give it your API key from the [dashboard](https://app.bland.ai/dashboard/settings) when it asks:

```
I want you to be able to make phone calls and send texts through Bland. The API base URL is https://api.bland.ai and every request needs an Authorization: Bearer header with my API key, which I'll send next. Store it with your other credentials. Read https://docs.bland.ai/platform/agent-quickstart to learn how to place a call and read the transcript.
```

Then ask it to make a call:

```
Call +1 415 555 0123, introduce yourself as my assistant, and ask what time they close today.
```

To give Instinct its own number, so it can receive calls and send texts, subscribe to the Agent Phone Plan at [Billing & Plans](https://app.bland.ai/dashboard/pay?tab=plan). The plan provisions a US number within a few seconds. Ask "what's my Bland phone number?" and Instinct reads it from your account and uses it from then on.

## Option 2: Let Instinct set itself up

If you do not have a Bland account, Instinct can request its own access. Text it:

```
Read https://docs.bland.ai/platform/connect-your-agent and get yourself a Bland phone number. Show me the link and code when you have them. Once approved, store the API key you receive with your other credentials and read https://docs.bland.ai/platform/agent-quickstart, then call me at +1 415 555 0123 to confirm it works.
```

Instinct starts the device flow, texts you a link and a short code, and waits. Open the link, sign up or sign in, subscribe to the Agent Phone Plan, and approve the request. Instinct receives its own API key and phone number and is ready to call and text.

The key is scoped to a new organization that belongs to you. Revoke it any time from **Settings > API Keys**.

## What Instinct can do

| Ask for | What happens |
| --- | --- |
| "Call +1 ... and ask ..." | Places an outbound call from Instinct's Bland number and returns the transcript and summary when it ends |
| "Text +1 ... saying ..." | Sends an SMS from Instinct's Bland number. Replies are answered automatically, so set the texting prompt first (see [Two-way texting](/integrations/mcp/personal-agents#two-way-texting)) |
| "What did they say on my last call?" | Reads the call transcript and analysis |

On the Agent Phone Plan, calls and texts are limited to US and Canadian numbers, one call at a time, up to 60 minutes per call, and up to 100 texts a day. See [Limits](/platform/agent-phone-plan#limits).

## Next steps

<CardGroup cols={2}>
  <Card title="Personal AI Agents" icon="robot" href="/integrations/mcp/personal-agents">
    Two-way texting, troubleshooting, and the general pattern.
  </Card>
  <Card title="Agent quickstart" icon="bolt" href="/platform/agent-quickstart">
    The REST calls Instinct makes: place a call, learn how it ended, read the transcript.
  </Card>
</CardGroup>

Docs for agents: [llms.txt](/llms.txt)
