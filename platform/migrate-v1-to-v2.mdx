---
title: "Migrate a v1 pathway to a v2 agent"
description: "A dedicated flow for converting an existing conversational pathway into a v2 agent: install the Bland plugin, drop in one prompt, and review the migrated agent."
---

v2 agents are the successor to conversational pathways: the same conversation engine, with a cleaner model built around scenarios, versioned snapshots, environments, and first-class testing. This page is the dedicated flow for moving a pathway across — you drop one prompt into Claude Code and an agent session does the migration end to end, with checkpoints where you stay in control.

<Note>
**Migration is additive.** It only ever creates new v2 agent versions. Your v1 pathway, its phone numbers, tools, snippets, and live traffic are never modified. You switch traffic to the new agent only when you decide to.
</Note>

## Pick your path

<CardGroup cols={3}>
  <Card title="Import in the dashboard" icon="bolt">
    **Fastest.** In the dashboard, create a new agent and choose **Import pathways**. A mechanical 1:1 conversion — good for simple pathways you want to start iterating on in v2 immediately.
  </Card>
  <Card title="Managed migration" icon="server">
    Bland runs the full agent-driven migration described below on our infrastructure. Start it from the **Migrate** option in the dashboard and watch live status; expect 30–90 minutes for a complex pathway.
  </Card>
  <Card title="In your Claude Code" icon="terminal">
    **This guide.** The same rigor as a managed migration, but you watch every step, approve the architecture, and can steer. Runs on your Claude subscription.
  </Card>
</CardGroup>

The rest of this page is the Claude Code flow.

## 1. Install the plugin

You need [Claude Code](https://claude.com/claude-code) and a Bland API key for the organization that owns the pathway ([app.bland.ai/settings/api-keys](https://app.bland.ai/settings/api-keys)).

Inside a Claude Code session:

```text
/plugin marketplace add CINTELLILABS/bland-plugins
/plugin install norm@bland
```

The install prompts for your API key and stores it outside the conversation. Scripted installs pass it on the command line instead:

```bash
claude plugin marketplace add CINTELLILABS/bland-plugins
claude plugin install norm@bland --config bland_api_key=YOUR_KEY
```

Restart the session after installing.

<Warning>
Never paste your API key into the chat. The install prompt and the `--config` flag keep it out of the model context. To set or rotate it later, run `/plugin configure norm@bland`.
</Warning>

`norm@bland` is the v2-focused plugin: it carries the full migration playbook, a deterministic snapshot builder and parity audit, and a convergence loop that won't declare the migration done until the checks are actually green. It talks to your account through the hosted [Bland MCP server](/integrations/mcp/overview) — there is nothing else to configure. (The general-purpose [`bland@bland` plugin](/integrations/mcp/norm) covers the v1 surface; install both if you work on both.)

## 2. Drop in the prompt

Find your pathway ID — it's in the pathway's URL in the dashboard. Then paste this, filled in:

```text
/norm:migrate Migrate my v1 pathway <PATHWAY_ID> into a new v2 agent named "<AGENT NAME>".
Fetch the pathway export through the Bland MCP server. Show me the proposed scenario
architecture before authoring anything, ask me before the first version push, and finish
with the full simulation suite and the migration report.
```

That's the whole flow. Variations:

- **Persona attached to the pathway?** Add its ID: `…and include my persona <PERSONA_ID>.`
- **Prefer to hand over files?** Export the pathway JSON and attach it instead of the ID: `/norm:migrate Migrate the attached v1 export into a new v2 agent named "…"`.
- **Migrating into an existing agent?** Say so — versions are additive, so a migration can land as a new version on an agent you already created.

## 3. What the session does

<Steps>
  <Step title="Discover">
    Censuses the export: every node and edge, webhooks and their auth, code snippets and their pinned versions, transfers, knowledge bases, and the variables your callers' request data must supply.
  </Step>
  <Step title="Architect — you approve">
    Proposes the v2 shape: which nodes become which scenarios, where the hub routes, what each entry means. It presents this before authoring anything; this is your main steering point.
  </Step>
  <Step title="Author">
    A deterministic builder carries every byte of your content — prompts, route rules, webhook configs, snippet pins, tools — from the v1 export into the agent snapshot. Your prompts arrive verbatim, not paraphrased. Known v1 quirks are carried bug-for-bug so behavior doesn't silently change.
  </Step>
  <Step title="Audit">
    A machine parity audit checks the snapshot against your v1 source — structure, routing, byte-level prompt carriage, and that every snippet and tool made it across — before any write to your account.
  </Step>
  <Step title="Push — asks first">
    Pushes the audited snapshot as a new agent version. This is the first write, and the session asks before doing it.
  </Step>
  <Step title="Simulate">
    Designs and runs a simulation suite over the migrated agent's lanes — intake, transfers, voicemail, edge cases — and grades results on engine traces, not vibes. Failures loop back into fixes; the loop only completes when the suite is green on the exact bytes that were pushed.
  </Step>
  <Step title="Report">
    A migration report: what mapped where, documented behavioral deltas, carried v1 quirks, the request-data contract, and — honestly — any lanes that can only be verified with your real data.
  </Step>
</Steps>

Expect a complex pathway to take a while — the session is mostly hands-off after you approve the architecture, and it will not report success it can't prove.

## 4. After the migration

- **Read the report**, especially the "needs verification with your data" list — lanes gated on your CRM records or live integrations that simulations couldn't exercise safely.
- **Test-chat the agent** in the dashboard against your real scenarios.
- **Move traffic when ready.** Attach or move phone numbers in the dashboard — the migration never touches routing, so your v1 pathway keeps answering until you switch.

## Using the MCP server without the plugin

Any MCP client can connect to the Bland MCP server directly — `https://api.bland.ai/v1/mcp` with your API key as a bearer token (see [MCP clients](/integrations/mcp/clients)). That gives an agent the raw tools: reading pathways, creating agents, pushing versions, running tests. The migration playbook, parity audit, and convergence loop ship in the plugin, though — for migrations, use the plugin.
