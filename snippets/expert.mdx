export const BlandExpert = ({ name, specialism, description, logo, website, discord }) => (
  <div style={{ display: 'flex', flexDirection: logo ? 'row' : 'column', alignItems: logo ? 'flex-start' : 'left', marginBottom: '10px' }}>
    {logo && (
      <div style={{ flex: '0 0 auto', marginRight: '20px' }}>
        <img src={logo} alt="Logo" style={{ maxWidth: '200px', width: '100%', margin: '0px' }} />
      </div>
    )}
    <div>
      <h1 style={{ margin: '0px' }}>{name}</h1>
      {specialism && (
        <p style={{ margin: '0px' }}>
          <blockquote style={{ margin: '0px' }}>
            {specialism}
          </blockquote>
        </p>
      )}
      {description && <p style={{marginTop: '8px', marginBottom: '8px' }}>{description}</p>}
      {website && <><a href={website}>Website</a>&nbsp;</>}
      {discord && <a href="https://discord.com/invite/QvxDz8zcKe">@{discord}</a> }
    </div>
  </div>
);