---
title: "Overview"
description: "Run conversational agents over SMS, RCS, and iMessage from one stack, powered by the same pathways, personas, and memory you use for voice."
---

# Introduction

Bland Messaging lets you run conversational agents over SMS, RCS, and iMessage from one place. The same pathways, personas, tools, memory, and analytics power every channel, so the flow you build for voice can drive your text conversations without rebuilding it.

<Info>
  **Enterprise feature** — Messaging is available on Enterprise plans. Contact your Account Executive or [reach out to sales](https://www.bland.ai/book-a-demo) to get set up.
</Info>

<CardGroup cols={2}>
  <Card title="Pathway-driven replies" icon="route">
    Use the same pathways and personas you already use for voice. Backchanneling and pause hints are stripped automatically so messages stay clean.
  </Card>
  <Card title="Cross-channel memory" icon="brain">
    A contact reached over SMS, iMessage, or voice keeps one shared history. Facts, summaries, and open items carry across channels.
  </Card>
  <Card title="Outcomes and citations" icon="chart-bar">
    The same outcomes, citation schemas, and summary prompts you use on voice run on every messaging conversation.
  </Card>
  <Card title="One API surface" icon="code">
    Send and receive across all three channels with the same `/v1/sms/*` endpoints. Channel is configured per number or selected per request.
  </Card>
</CardGroup>

## Channels

Each channel has different reach and capabilities. You can use one number for one channel, or layer them so the same contact escalates to richer messaging when supported.

<CardGroup cols={3}>
  <Card title="iMessage" icon="comment" href="/tutorials/messaging/imessage">
    Branded contact on Apple devices with photos, videos, voice memos, tapbacks, text effects, and end-to-end encryption.
  </Card>
  <Card title="RCS" icon="message-dots" href="/tutorials/messaging/rcs">
    Verified business profile, suggested replies, action buttons, cards, and media on supported Android devices and carriers.
  </Card>
  <Card title="SMS" icon="message" href="/tutorials/messaging/sms">
    Universal reach in the US. Plain text. Requires A2P registration.
  </Card>
</CardGroup>

If your number is provisioned for RCS or iMessage, Bland uses the richest channel the recipient supports and falls back when needed.

## Attaching a pathway or persona

Pathways and personas attach to messaging numbers the same way they attach to phone numbers for calls. From the [Messaging dashboard](https://app.bland.ai/dashboard/sms), pick a number and configure it with a [pathway](/tutorials/pathways) or a [persona](/tutorials/personas). The number then handles inbound messages using the same logic, tools, and memory you use on voice.

If you already have a voice pathway, you can clone it for messaging and use the **Sync** action to keep both in sync as you iterate.

## Memory across conversations

[Memory](/tutorials/memories) can be enabled on any messaging channel and is shared with your voice agents. When memory is on, the same contact is recognized across SMS, RCS, iMessage, and voice, with one coherent history of facts, summaries, and open items. For example, an agent can remember how a customer wants a report formatted, then reuse that preference the next time the customer reaches out, regardless of which channel they use.

## Conversation lifecycle

When a conversation ends (via timeout, end-call node, or opt-out), the agent stops responding. Restart by deleting the conversation or its messages in the [messaging dashboard](https://app.bland.ai/dashboard/sms), then start a new thread.

Per-number and per-request settings let you control:

- **Timeouts.** When the user goes silent for a configured period, the agent can send a warning message and end the conversation.
- **Restart on reply.** When `restart_after_end_call` is enabled, a new conversation starts automatically if the user replies after the previous one ended.
- **Webhooks.** Each agent reply (and the conversation status) can be POSTed to a URL of your choice with the full message context.

## Exporting conversations

From the [Messaging dashboard](https://app.bland.ai/dashboard/sms), click **Export Conversations** in the header to export your messaging history as a CSV delivered via email.

Configure your export with:

- **Email** — where to send the CSV (defaults to your account email)
- **Timezone** — for timestamp formatting
- **Date range** — up to 180 days
- **Exclude columns** — choose which fields to include or exclude (transcript, citation variables, variables, and more)

Active filters on the dashboard apply to the export, so you can narrow by date, status, pathway, contact, or any other supported filter before exporting. Each row in the CSV represents one conversation and includes the full transcript joined into a single column, the computed status, and the total message count.

Exports run in the background. When the file is ready, you receive an email with a signed download link that stays valid for 24 hours. If no conversations match your filters, the email tells you the export was empty.

You can run up to 20 exports per hour across call logs and conversations combined. The action requires an owner, admin, or operator role.

## Outcomes, citations, and summaries

Messaging conversations support the same post-conversation analytics as voice. When a conversation ends, Bland can:

- Run [outcomes](/tutorials/outcomes) to classify what happened.
- Extract [citations](/enterprise-features/citations) from the transcript.
- Generate a summary using a custom prompt.

These can be configured per number on the messaging config, or passed per request on `/sms/send`. Webhook payloads include a `channel` field set to `"sms"`, `"rcs"`, `"whatsapp"`, or `"imessage"` so you can route them to the right handler.

When a conversation ends, the status webhook includes the full conversation context:

- `variables` (pathway variables collected during the conversation)
- `metadata` (custom metadata passed when the conversation was created)
- `pathway_tags` (tags from pathway nodes visited during the conversation)
- `concatenated_transcript` (the full conversation transcript as a single string)
- `summary` (AI-generated summary, when a summary prompt is configured)
- `citations` (extracted citation variables, when citation schemas are configured)
- `dispositions` (outcome run results, when outcomes are configured)

Message webhooks also include `variables`, `metadata`, `pathway_tags`, and `created_at` for parity with voice call webhooks.

## API

All three channels share the same endpoints. Pass `channel` in the request body, or let it be auto-derived from the number's configuration.

<CardGroup cols={2}>
  <Card title="Send a message" icon="paper-plane" href="/api-v1/post/sms-send">
    Send a message or start a conversation on any channel.
  </Card>
  <Card title="Create conversation" icon="comment" href="/api-v1/post/sms-create">
    Create or resume a conversation with a specific pathway, node, and version.
  </Card>
  <Card title="List numbers" icon="phone" href="/api-v1/get/sms-numbers">
    List your numbers and their channel configuration.
  </Card>
  <Card title="Conversations" icon="inbox" href="/api-v1/get/sms-conversations">
    List, retrieve, patch, and delete conversations.
  </Card>
</CardGroup>

For webhooks, delivery status, and number updates, see the **Messaging** section in the [API Reference](/api-v1/get/sms-numbers).

## Related

- [SMS](/tutorials/messaging/sms) (A2P registration and bring-your-own-Twilio)
- [RCS](/tutorials/messaging/rcs) (verified business profile and rich messaging)
- [iMessage](/tutorials/messaging/imessage) (branded iMessage agents with rich media)
- [Personas](/tutorials/personas) (one agent across voice and every messaging channel)
- [Memory](/tutorials/memories) (cross-channel contact recognition)
- [Outcomes](/tutorials/outcomes) (classify what happened in each conversation)
- [Post-Call Webhooks](/tutorials/post-call-webhooks) (webhook payloads for voice and messaging)
- [Billing](/platform/billing#sms-billing) (SMS pricing and usage)

---

Docs for agents: [llms.txt](/llms.txt)
